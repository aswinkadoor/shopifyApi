Step 1 

Run the Application it load the index page having no data.

Step 2

We have to sync the api data and save to DB by click on Sync Button in UI

On Click the application accepts API data from the speicified URL and save to DB and load the index page

and the same has been visible in UI

Step 3 

Check the DB for number of row inserted.

Step 4

Filter option is given in UI to check the data.

Step 5 

Date filter is given find transaction between seleted date ranges.

Step 6

Sync data from API click in sync button will accpet data from API and check with existing data in DB and only
new data is update into DB

